import 'package:flutter/material.dart';

/// Change Mint here so every widget that uses `Mint` picks up the new sky color.
const Color Mint = Color(0xFF9ED8F6); // soft sky-blue
const Color MintDark = Color(0xFF2E8FAF);
const Color WhiteColor = Color(0xFFFFFFFF);
const Color Accent = Color(0xFFFFA726);
const Color BlackColor = Color(0xFF000000);